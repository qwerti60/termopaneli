/// Флаги возможностей приложения (в т.ч. отключённые без удаления кода).
///
/// **`kHomeScreenEnabled`** — отдельный экран «Дом» (`HomeScreen`) и пункт в ЛК.
/// При `false` пункт в профиле скрыт; маршрут **`/home`** в [AppRouter.onGenerateRoute]
/// открывает тот же экран, что **`/panel-fit`** (Дом/Примерка). Чтобы вернуть хаб
/// «Мои фото» / «Наши дома», поставьте `true` в этом файле и оставьте пункт в
/// `ProfileScreen`.
const bool kHomeScreenEnabled = false;
